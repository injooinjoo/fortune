{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@10.33.1 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @fortune/web build",
  "outputDirectory": "apps/web/.next"
}
