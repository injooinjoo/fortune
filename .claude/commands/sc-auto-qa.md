개발 완료 후 자동으로 실제 앱을 테스트하는 QA를 실행합니다.

## 입력

`$ARGUMENTS` - 테스트 대상 (페이지 경로, 기능명, 또는 "all")

## 사전 조건

### 1. Flutter Web 서버 실행 확인

```bash
# 서버가 실행 중인지 확인
curl -s http://localhost:3000 > /dev/null && echo "✅ 서버 실행 중" || echo "❌ 서버 미실행"
```

서버가 실행 중이 아니면:
```bash
# 백그라운드에서 Flutter Web 실행
flutter run -d chrome --web-port=3000 &
# 서버 준비 대기 (약 60-90초)
```

### 2. Playwright 설치 확인

```bash
npx playwright install chromium
```

## 테스트 실행 프로세스

### Step 1: 테스트 대상 식별

| 입력 | 테스트 범위 |
|------|------------|
| `/fortune/daily` | 일일운세 페이지 |
| `/fortune/tarot` | 타로 페이지 |
| `daily_fortune_page` | 해당 파일 관련 테스트 |
| `all` | 전체 E2E 테스트 |
| (미입력) | 최근 변경된 페이지 자동 감지 |

### Step 2: 테스트 시나리오 선택

**운세 페이지 테스트** (`fortune-page` 시나리오):
1. 페이지 로딩 확인
2. 입력 폼 렌더링 확인
3. 운세 생성 버튼 클릭
4. 로딩 상태 확인
5. 결과 화면 렌더링 확인
6. 블러/프리미엄 상태 확인
7. 스크린샷 캡처

**일반 페이지 테스트** (`page-render` 시나리오):
1. 페이지 로딩 확인
2. 주요 UI 요소 렌더링 확인
3. 네비게이션 동작 확인
4. 콘솔 에러 확인
5. 스크린샷 캡처

### Step 3: Playwright MCP 실행

```javascript
// Playwright MCP 활용하여 테스트 실행
// 1. 브라우저 열기
// 2. 테스트 계정으로 인증 (BYPASS_AUTH)
// 3. 대상 페이지 이동
// 4. 테스트 시나리오 실행
// 5. 결과 수집
```

### Step 4: 결과 리포트

```
============================================
🎭 자동 QA 테스트 결과
============================================

📍 테스트 대상: $ARGUMENTS
🕐 실행 시간: {timestamp}
🌐 테스트 URL: http://localhost:3000/{path}

테스트 항목:
┌─────────────────────────┬────────┬─────────┐
│ 항목                    │ 결과   │ 시간    │
├─────────────────────────┼────────┼─────────┤
│ 페이지 로딩             │ ✅     │ 2.3s    │
│ UI 요소 렌더링          │ ✅     │ 0.5s    │
│ 인터랙션 테스트         │ ✅     │ 1.2s    │
│ API 응답                │ ✅     │ 0.8s    │
│ 콘솔 에러               │ ✅     │ -       │
└─────────────────────────┴────────┴─────────┘

📸 스크린샷: playwright/screenshots/{test-name}.png

============================================
총 결과: ✅ PASS (5/5 항목)
============================================
```

## 테스트 시나리오 템플릿

### 운세 페이지 시나리오

```javascript
// playwright/scenarios/fortune-page.scenario.js
module.exports = {
  name: 'fortune-page',
  steps: [
    { action: 'goto', url: '/{path}' },
    { action: 'waitForLoadState', state: 'networkidle' },
    { action: 'waitForTimeout', ms: 3000 },
    { action: 'assertVisible', selector: 'text=운세' },
    { action: 'click', selector: 'button:has-text("운세")' },
    { action: 'waitForSelector', selector: '[class*="loading"]', state: 'hidden' },
    { action: 'assertVisible', selector: 'text=결과' },
    { action: 'screenshot', name: '{path}-result' }
  ]
};
```

### 폼 입력 시나리오

```javascript
// playwright/scenarios/form-input.scenario.js
module.exports = {
  name: 'form-input',
  steps: [
    { action: 'goto', url: '/{path}' },
    { action: 'fill', selector: '[type="date"]', value: '1990-01-01' },
    { action: 'selectOption', selector: 'select', value: '자시' },
    { action: 'click', selector: 'button[type="submit"]' },
    { action: 'waitForResponse', urlPattern: '**/functions/**' },
    { action: 'assertVisible', selector: 'text=결과' }
  ]
};
```

## 실패 시 조치

### 서버 미실행

```
❌ 서버가 실행되지 않았습니다.

다음 명령어로 서버를 시작하세요:
flutter run -d chrome --web-port=3000

서버 시작 후 다시 /sc:auto-qa를 실행하세요.
```

### 테스트 실패

```
❌ 테스트 실패: {실패 항목}

원인 분석:
- {에러 메시지}
- {스택 트레이스 요약}

권장 조치:
1. 콘솔에서 에러 로그 확인
2. 해당 컴포넌트 코드 검토
3. /sc:analyze-error로 근본 원인 분석
```

## CLI 명령어

```bash
# 특정 페이지 테스트
npx playwright test -g "daily fortune"

# 전체 E2E 테스트
npx playwright test playwright/tests/e2e/

# 디버그 모드 (브라우저 표시)
npx playwright test --headed --debug

# 특정 브라우저
npx playwright test --project=chromium
```

## 자동 실행 트리거

다음 작업 완료 후 자동으로 실행됩니다:

| 작업 | 트리거 조건 |
|------|------------|
| 운세 페이지 수정 | `*_fortune_page.dart` 파일 변경 |
| UI 컴포넌트 수정 | `presentation/widgets/` 파일 변경 |
| Edge Function 배포 | `supabase functions deploy` 후 |
| 프리미엄/블러 수정 | `blur`, `premium` 관련 코드 변경 |

## 관련 Agent

- `playwright-qa-agent` - E2E 테스트 전문가
- `testing-architect` - 테스트 설계
- `error-resolver` - 실패 분석

## 관련 문서

- [16-auto-qa-system.md](../docs/16-auto-qa-system.md)
- [playwright.config.js](../../playwright.config.js)