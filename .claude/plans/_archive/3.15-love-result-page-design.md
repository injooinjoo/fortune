# [3.15] 연애 결과페이지 디자인 개선 기획서

## 1. 요구사항

| 항목 | 설명 | 복잡도 |
|------|------|--------|
| 3.15.1 | 연애운 결과 페이지 UI/UX 개선 | 중간 |
| 3.15.2 | 시각적 요소 강화 (그래프, 차트 등) | 중간 |

---

## 2. 현황 분석

### 2.1 현재 구현

**페이지 구조**:
- 메인 점수 카드 (공개)
- 연애 성향 (공개)
- 매력 포인트 (공개)
- 개선 포인트 (공개)
- 궁합 인사이트 (프리미엄)
- 미래 예측 (프리미엄)
- 실천 계획 (프리미엄)
- 주의사항 (프리미엄)

**총 8개 섹션, 4개 프리미엄**

### 2.2 관련 파일
```
lib/features/fortune/presentation/pages/love/
├── love_fortune_result_page.dart (결과 페이지)
└── widgets/ (하위 위젯 없음 - 인라인 구현)

lib/features/fortune/presentation/pages/love_fortune_input/
└── love_fortune_input_page.dart (입력 페이지)
```

### 2.3 현재 레이아웃

```
┌─────────────────────────────────────┐
│  [X 닫기]                           │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐    │
│  │     💕 78점                 │    │ ← 메인 점수 (핑크 그래디언트)
│  │   "설레는 만남이 기다려요"   │    │
│  └─────────────────────────────┘    │
│                                     │
│  [💝 연애 성향]                      │
│  텍스트...                          │
│                                     │
│  [✨ 매력 포인트]                    │
│  텍스트...                          │
│                                     │
│  [🎯 개선 포인트]                    │
│  텍스트...                          │
│                                     │
│  ┌─ 프리미엄 섹션 ─┐                    │
│  │ [💜 궁합 인사이트] │              │
│  │ [🔮 미래 예측]    │              │
│  │ [📋 실천 계획]    │              │
│  │ [⚠️ 주의사항]     │              │
│  └────────────────┘                 │
│                                     │
│  [연애 조언 모두 보기] ← 프리미엄 잠금 해제│
└─────────────────────────────────────┘
```

---

## 3. 개선 방향

### 3.1 디자인 컨셉

**로맨틱 & 모던**:
- 핑크/레드 그래디언트 유지
- 하트/꽃 모티프 추가
- 부드러운 곡선 테두리
- 애니메이션 강화

### 3.2 개선 UI 레이아웃

```
┌─────────────────────────────────────┐
│  💕 연애운 결과               [X]   │
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐    │
│  │    ❤️ 78 / 100              │    │
│  │    ━━━━━━━━━━━━━━━━━━━━━━    │ ← 프로그레스 바 추가
│  │  "설레는 만남이 기다려요"    │    │
│  │                             │    │
│  │  [💘 열정 85] [💕 감성 72]   │    │ ← 세부 점수 태그 추가
│  │  [🤝 신뢰 78] [💬 소통 80]   │    │
│  └─────────────────────────────┘    │
│                                     │
│  ┌── 레이더 차트 ──┐                │ ← 새로운 시각화
│  │    열정    │                     │
│  │     *     │                     │
│  │ 신뢰  *  감성│                   │
│  │     *     │                     │
│  │    소통    │                     │
│  └─────────────┘                    │
│                                     │
│  💝 연애 성향                        │
│  ┌─────────────────────────────┐    │
│  │ 당신은 [로맨티스트] 타입!    │    │ ← 타입 배지 추가
│  │ 감성적이고 섬세한...         │    │
│  └─────────────────────────────┘    │
│                                     │
│  ✨ 매력 포인트                      │
│  ┌──────┐ ┌──────┐ ┌──────┐        │
│  │ 유머 │ │ 배려 │ │ 센스 │ ← 태그 형태│
│  └──────┘ └──────┘ └──────┘        │
│                                     │
│  ... (프리미엄 섹션)                    │
│                                     │
│  [💕 연애 조언 모두 보기]            │
└─────────────────────────────────────┘
```

---

## 4. 상세 구현

### 4.1 메인 점수 카드 개선

**현재**:
```dart
Container(
  decoration: BoxDecoration(
    gradient: LinearGradient(
      colors: [Color(0xFFFF6B6B), Color(0xFFFF8CC8)],
    ),
  ),
  child: Column(
    children: [
      Text('💕 $score점'),
      Text(message),
    ],
  ),
)
```

**개선**:
```dart
Container(
  decoration: BoxDecoration(
    gradient: LinearGradient(
      colors: [Color(0xFFFF6B6B), Color(0xFFFF8CC8)],
    ),
    borderRadius: BorderRadius.circular(20),
  ),
  child: Column(
    children: [
      // 점수 + 프로그레스 바
      Row(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          Text('❤️', style: TextStyle(fontSize: 32)),
          SizedBox(width: 8),
          Text('$score', style: scoreStyle),
          Text(' / 100', style: maxScoreStyle),
        ],
      ),

      // 프로그레스 바
      LinearProgressIndicator(
        value: score / 100,
        backgroundColor: Colors.white24,
        valueColor: AlwaysStoppedAnimation(Colors.white),
      ),

      // 메시지
      Text(message, style: messageStyle),

      // 세부 점수 태그
      Wrap(
        spacing: 8,
        children: [
          _buildSubScoreTag('💘 열정', passionScore),
          _buildSubScoreTag('💕 감성', emotionScore),
          _buildSubScoreTag('🤝 신뢰', trustScore),
          _buildSubScoreTag('💬 소통', communicationScore),
        ],
      ),
    ],
  ),
)
```

### 4.2 레이더 차트 추가

```dart
// fl_chart 패키지 사용
Widget _buildRadarChart() {
  return RadarChart(
    RadarChartData(
      dataSets: [
        RadarDataSet(
          fillColor: Colors.pink.withOpacity(0.3),
          borderColor: Colors.pink,
          dataEntries: [
            RadarEntry(value: passionScore.toDouble()),
            RadarEntry(value: emotionScore.toDouble()),
            RadarEntry(value: trustScore.toDouble()),
            RadarEntry(value: communicationScore.toDouble()),
          ],
        ),
      ],
      radarBackgroundColor: Colors.transparent,
      titleTextStyle: TextStyle(fontSize: 12),
      getTitle: (index, angle) {
        switch (index) {
          case 0: return RadarChartTitle(text: '열정');
          case 1: return RadarChartTitle(text: '감성');
          case 2: return RadarChartTitle(text: '신뢰');
          case 3: return RadarChartTitle(text: '소통');
          default: return RadarChartTitle(text: '');
        }
      },
    ),
  );
}
```

### 4.3 타입 배지

```dart
Widget _buildTypeBadge(String type) {
  return Container(
    padding: EdgeInsets.symmetric(horizontal: 16, vertical: 8),
    decoration: BoxDecoration(
      gradient: LinearGradient(
        colors: [Color(0xFFFF6B9D), Color(0xFFFFA8C5)],
      ),
      borderRadius: BorderRadius.circular(20),
    ),
    child: Row(
      mainAxisSize: MainAxisSize.min,
      children: [
        Icon(Icons.favorite, size: 16, color: Colors.white),
        SizedBox(width: 4),
        Text(type, style: badgeStyle),
      ],
    ),
  );
}
```

### 4.4 매력 포인트 태그

```dart
Widget _buildCharmTags(List<String> charms) {
  return Wrap(
    spacing: 8,
    runSpacing: 8,
    children: charms.map((charm) => Container(
      padding: EdgeInsets.symmetric(horizontal: 12, vertical: 6),
      decoration: BoxDecoration(
        color: Colors.pink.withOpacity(0.1),
        borderRadius: BorderRadius.circular(16),
        border: Border.all(color: Colors.pink.withOpacity(0.3)),
      ),
      child: Text(charm, style: charmStyle),
    )).toList(),
  );
}
```

---

## 5. 필요 변경사항

### 5.1 API 응답 확장 (선택)

현재 API 응답에 세부 점수가 없다면 추가 필요:
```json
{
  "score": 78,
  "subScores": {
    "passion": 85,
    "emotion": 72,
    "trust": 78,
    "communication": 80
  },
  "type": "로맨티스트",
  "charms": ["유머 감각", "배려심", "센스"]
}
```

### 5.2 패키지 추가

- `fl_chart`: 레이더 차트용 (선택)
- 또는 CustomPaint로 직접 구현

---

## 6. 체크리스트

- [ ] 메인 점수 카드 UI 개선 (프로그레스 바, 세부 점수)
- [ ] 연애 타입 배지 추가
- [ ] 매력 포인트 태그 형태로 변경
- [ ] (선택) 레이더 차트 추가
- [ ] 색상 팔레트 통일 (핑크/레드 그래디언트)
- [ ] 애니메이션 추가 (FadeIn, SlideUp)
- [ ] Edge Function 응답 확장 (필요 시)
- [ ] flutter analyze 검증

---

## 7. 예상 소요 시간

| 항목 | 예상 시간 |
|------|----------|
| 메인 점수 카드 개선 | 30분 |
| 타입 배지 추가 | 15분 |
| 매력 포인트 태그 | 15분 |
| 레이더 차트 (선택) | 45분 |
| 애니메이션 | 20분 |
| API 수정 (필요 시) | 30분 |
| 테스트 | 15분 |
| **합계** | **170분** |

---

## 8. 앱 내 유사 구현 참조

### Biorhythm 레이더 차트
```
lib/features/fortune/presentation/pages/biorhythm/biorhythm_result_page.dart
```
- fl_chart 사용 예시
- 레이더 차트 패턴

### MBTI 점수 카드
```
lib/features/fortune/presentation/pages/mbti_fortune/widgets/energy_card.dart
```
- 에너지 레벨 시각화
- 프로그레스 바 패턴

### Saju 인사이트 카드
```
lib/screens/home/fortune_swipe/widgets/saju_insight_card.dart
```
- 태그 형태 UI
- 그리드 레이아웃

---

**작성일**: 2025-12-17