<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="@drawable/widget_background">

    <!-- Header with Icon and Category Name -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="8dp">

        <TextView
            android:id="@+id/category_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="💕"
            android:textSize="20sp"
            android:layout_marginEnd="6dp" />

        <TextView
            android:id="@+id/category_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="연애운"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="@color/widget_title_color" />
    </LinearLayout>

    <!-- Score -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginBottom="6dp">

        <TextView
            android:id="@+id/category_score"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="78"
            android:textSize="36sp"
            android:textStyle="bold"
            android:textColor="@color/widget_score_color" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="점"
            android:textSize="14sp"
            android:textColor="@color/widget_subtitle_color"
            android:layout_marginStart="4dp" />
    </LinearLayout>

    <!-- Category Message -->
    <TextView
        android:id="@+id/category_message"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:text="마음을 열고 상대방의 이야기에 귀 기울이세요."
        android:textSize="12sp"
        android:textColor="@color/widget_text_color"
        android:maxLines="2"
        android:ellipsize="end"
        android:gravity="center" />

    <!-- Engagement Badge -->
    <TextView
        android:id="@+id/engagement_badge"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="오늘의 연애운 확인 💕"
        android:textSize="11sp"
        android:textColor="#FFFFFF"
        android:background="@drawable/engagement_badge_bg"
        android:padding="5dp"
        android:gravity="center"
        android:visibility="gone" />

</LinearLayout>
