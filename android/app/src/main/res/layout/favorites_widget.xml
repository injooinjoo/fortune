<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@drawable/widget_background">

    <!-- Header with Icon and Title -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="8dp">

        <!-- Fortune Icon -->
        <TextView
            android:id="@+id/widget_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="✨"
            android:textSize="24sp"
            android:layout_marginEnd="8dp" />

        <!-- Fortune Title -->
        <TextView
            android:id="@+id/widget_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="일일운세"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/widget_title_color"
            android:maxLines="1"
            android:ellipsize="end" />

        <!-- Rolling Indicator -->
        <TextView
            android:id="@+id/rolling_indicator"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="1/3"
            android:textSize="11sp"
            android:textColor="@color/widget_subtitle_color"
            android:background="@drawable/widget_button_background"
            android:paddingHorizontal="8dp"
            android:paddingVertical="2dp"
            android:visibility="gone" />
    </LinearLayout>

    <!-- Score Display -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginBottom="12dp">

        <TextView
            android:id="@+id/widget_score"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="85"
            android:textSize="44sp"
            android:textStyle="bold"
            android:textColor="@color/widget_score_color" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="점"
            android:textSize="18sp"
            android:textColor="@color/widget_subtitle_color"
            android:layout_marginStart="4dp" />
    </LinearLayout>

    <!-- Extra Info (Type-specific data) -->
    <TextView
        android:id="@+id/widget_extra_info"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="🎨 파란색  🔢 7"
        android:textSize="12sp"
        android:textColor="@color/widget_text_color"
        android:gravity="center"
        android:layout_marginBottom="8dp"
        android:background="@drawable/widget_lucky_items_background"
        android:padding="6dp"
        android:visibility="gone" />

    <!-- Fortune Message -->
    <TextView
        android:id="@+id/widget_message"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:text="오늘은 새로운 기회가 찾아올 예정입니다."
        android:textSize="13sp"
        android:textColor="@color/widget_text_color"
        android:gravity="center"
        android:maxLines="3"
        android:ellipsize="end" />

</LinearLayout>
