# Figma Sync Changelog

This file is the repository-side proof that a code change was reconciled with the official Figma file.

## Rules

1. Any route change must update:
   - `playwright/scripts/figma_capture_manifest.js`
   - `docs/design/FIGMA_LAYER_NAMING_STANDARD.md` when the layer contract changes
   - `docs/design/FIGMA_SOURCE_OF_TRUTH.md`
   - `docs/design/FIGMA_SCREEN_ROUTE_MAPPING.md`
   - `docs/design/FIGMA_SCREEN_COMPONENT_REGISTRY.md`
   - this changelog
2. Any UI surface change under tracked presentation, screen, shared, or design-system files must append a changelog entry even if the screen list did not change.
3. The official Figma file remains [Fortune Screen Catalog - Official](https://www.figma.com/design/dkx3Biwe5xkiMQWsjq95LA).
4. CI runs `npm run figma:guard` and fails when these records are missing.

## Entries

| Date | Jira | Code Scope | Affected Screens / Components | Figma Action |
| --- | --- | --- | --- | --- |
| 2026-03-24 | `KAN-180` | Shared SmartImage SVG path support | `chat__result__fortune_complete`, `Conversation, Survey, and Result Blocks` | Reconciled the shared runtime asset-rendering contract so governed chat/result surfaces can accept SVG assets through `SmartImage` without forcing the repository's illustration, brush, and texture assets out of raster form; no route or official Figma page inventory changed, and the repo-side component/source registry now explicitly covers `smart_image.dart` for future sync checks |
| 2026-03-24 | `KAN-178` | Chat survey camera/gallery action chip tone refinement | `chat__survey__fortune_step`, `Character Chat`, `Conversation, Survey, and Result Blocks` | Reconciled the existing chat survey image-input pills after the camera/gallery labels felt visually flat in runtime; warmed the tracked chip foreground, border, and fill treatment to match the survey surface better, and registered `chat_face_reading_flow.dart` plus `chat_image_input.dart` under the existing governed chat survey/component coverage without changing the official Figma page inventory |
| 2026-03-24 | `KAN-175` | Character chat archived-history viewport retention | `chat__survey__fortune_step`, `chat__result__fortune_complete`, `Character Chat`, `Conversation, Survey, and Result Blocks` | Reconciled the existing Character Chat runtime shell after archived history reveal kept snapping the viewport to the newly inserted upper messages; preserved the reader's current message position while keeping older history reachable by manual upward scroll, with no route, capture inventory, or official Figma page changes required for this behavior-only parity fix |
| 2026-03-23 | `KAN-171` | Residual Haneul lucky-label localization fallback | `chat__result__fortune_complete`, `Curiosity Result Complete`, `Conversation, Survey, and Result Blocks` | Reconciled the existing Haneul curiosity result surface after a remaining lucky-item fallback path could still expose English payload keys in the governed chat card; registered the shared `fortune_key_localizer.dart` helper as part of the tracked result-surface source set and kept the current official Figma page inventory unchanged for this copy-only parity fix |
| 2026-03-23 | `KAN-169` | Character chat bottom-edge rebound clamp | `chat__survey__fortune_step`, `chat__result__fortune_complete`, `Character Chat`, `Conversation, Survey, and Result Blocks` | Reconciled the existing Character Chat runtime shell after users could reach the lower edge and rebound upward over the latest content/composer; clamped the timeline scroll physics at the bottom boundary so the governed chat surface stays anchored to the newest message without changing routes, layer contracts, or the official Figma page inventory in this turn |
| 2026-03-21 | `KAN-158` | Curiosity result body source coverage and iPhone Apple sign-in review fix | `chat__result__fortune_complete`, `Character Chat`, `Conversation, Survey, and Result Blocks` | Locked iPhone Apple sign-in back onto the native path for review reliability, then registered the decomposed curiosity result body widgets under the existing governed chat result surface so CI Figma coverage matches the already-shipped rich mystical card composition without changing the official page inventory |
| 2026-03-20 | `KAN-150` | Chat surface token dedup for Figma/runtime parity | `chat__home__general_default`, `chat__survey__fortune_step`, `chat__result__fortune_complete`, `Conversation, Survey, and Result Blocks` | Consolidated repeated bubble, typing, and survey wrapper values into a shared presentation-local chat surface style helper so the runtime chat shell keeps the current official Figma shape, shadow, and radius contract without reintroducing duplicate hardcoded values; no route or official page inventory changed |
| 2026-03-20 | `KAN-148` | Unread message badge numeral contrast fix | `chat__home__general_default`, `chat__home__curiosity_default`, `Design System Core`, `ds_badge` | Reconciled the governed message-list unread badge so error-count numerals use contrast-aware foreground color selection, keeping the existing red badge fill while aligning the shared `DSBadge` component with the tracked chat list surface contract; no route or official Figma page changed, but the repo-side design-system source registry now explicitly covers `ds_badge.dart` for future sync checks |
| 2026-03-20 | `KAN-145` | Splash screen visual cleanup with dark-only parity | `auth__splash__default`, `Splash` | Reconciled the runtime splash surface by removing the light/dark visual split and standardizing the screen on the existing dark presentation tokens and dark logo asset; no route or tracked surface inventory changed, and the placeholder splash capture remains timing-gated until a deterministic runtime capture is appended to the official file |
| 2026-03-17 | `KAN-133` | Fortune-cookie result card localization cleanup in character chat | `chat__result__fortune_complete`, `Character Chat`, `Conversation, Survey, and Result Blocks` | Reconciled the existing Haneul curiosity result surface after replacing English fallback eyebrow copy and lucky-item labels with Korean text on the governed fortune-cookie card; no route, layer contract, or capture range changed, and the current `20 Chat Home / Character` source page at `181:2` remains the official source of truth |
| 2026-03-17 | `KAN-127` | Rich mystical redesign for Haneul fortune chat cards | `chat__survey__fortune_step`, `chat__result__fortune_complete`, `Character Chat`, `Conversation, Survey, and Result Blocks` | Expanded the Haneul curiosity result card into a richer mystical single-card experience that surfaces category scores, time rhythm, lucky sets, action prompts, and longer reading sections from the existing API payloads, regenerated the chat captures, and appended the refreshed `20 Chat Home / Character` page into the official file at `181:2`, superseding `180:2` for the Haneul survey/result source nodes |
| 2026-03-17 | `KAN-124` | Premium redesign for Haneul fortune chat shell | `chat__survey__fortune_step`, `chat__result__fortune_complete`, `Character Chat`, `Conversation, Survey, and Result Blocks` | Reworked the Haneul curiosity survey/result shell into a denser premium-calm layout, shortened the card-first timeline by removing the extra analyzing/synthetic request bubble in the Haneul flow, regenerated the chat captures, and appended the refreshed `20 Chat Home / Character` page into the official file at `180:2`, superseding `95:2` for the Haneul survey/result source nodes |
| 2026-03-17 | `KAN-122` | Selected button/chip tone cleanup across chat survey, calendar, match selection, account deletion, and shared design-system chips | `chat__survey__fortune_step`, `account__deletion__auth_gated`, `Conversation, Survey, and Result Blocks`, `Account, Premium, and Policy Controls`, `Design System Core` | Reconciled the repo-side Figma tracking contract for the existing governed surfaces after softening selected-state controls from hard monochrome emphasis to lighter bordered pills; no new screen surface was introduced, but the tracked source registry now explicitly covers the shared chip core plus the calendar and match-selection widgets that define the selection treatment |
| 2026-03-16 | `KAN-103` | Curiosity chat top-start manual scroll release | `chat__survey__fortune_step`, `chat__result__fortune_complete`, `Character Chat`, `Conversation, Survey, and Result Blocks` | Re-verified the curiosity survey runtime against the official file `dkx3Biwe5xkiMQWsjq95LA` node `95:143`, then updated the top-start anchor behavior so the initiating user bubble still lands near the top while a manual upward scroll cancels the pending anchor/follow loop and keeps the opening bubbles reachable |
| 2026-03-16 | `KAN-100` | Logged-in account/settings route restoration and profile IA recovery | `account__profile__default`, `/profile`, `/profile/edit`, `/profile/saju-summary`, `/profile/relationships`, `Account, Premium, and Policy Controls` | Replaced the deprecated chat account sheet contract with a routed authenticated profile hub, restored the main profile IA and supporting subroutes in runtime, promoted `account__profile__default` into the official tracked surface set, and rebased the repo-side Figma mapping/docs so the restored `/profile` flow is governed again while subroutes remain folded into the main account card until their dedicated captures are added |
| 2026-03-15 | `KAN-93` | Chat message list chrome scroll trigger fix | `chat__home__general_default`, `chat__home__curiosity_default`, `Chat Shell and Headers`, `character_list_panel` | Reconciled the message list shell motion behavior after RCA showed the original notification-based trigger did not fire on real drag paths; updated the tracked chat header contract to use scroll offset changes as the source of truth and added a regression test for the collapse/reveal interaction |
| 2026-03-14 | `KAN-92` | Chat message list scroll-reactive chrome collapse | `chat__home__general_default`, `chat__home__curiosity_default`, `Chat Shell and Headers`, `character_list_panel` | Reconciled the message list shell behavior with the existing chat header contract by documenting the new scroll-driven hide/show motion for the page title and story/curiosity toggle; no route, manifest, or surface inventory changes were required |
| 2026-03-14 | `KAN-87` | Shared card taxonomy and reusable card family | `character_list_panel`, `insight_history_card`, `today_iljin_card`, `chat_saju_result_card`, `app_card`, `custom_card`, shared `fortune_cards` family | Consolidated the current card patterns into a shared component family, documented the card taxonomy, and aligned tracked component inventory so future screen/card refresh work can reuse a stable card shell set |
| 2026-03-14 | `KAN-86` | `/chat` catalog preview contract and live chat-page refresh | `chat__home__general_default`, `chat__home__curiosity_default`, `chat__survey__fortune_step`, `chat__result__fortune_complete`, appended chat refresh page `95:2` | Added deterministic `catalogState` preview routes on `/chat`, regenerated the four live captures, appended the refreshed `20 Chat Home / Character` page into the official file at `95:2`, and updated repo governance docs to coverage `18 / 14 / 4` |
| 2026-03-14 | `KAN-85` | Official catalog final parity refresh | `89:2` through `94:2`, `auth__callback__redirected`, splash placeholder gating, premium live capture, cover governance/runtime-blocker copy | Regenerated the official catalog from the current route manifest, appended the refreshed current set into the official file, deleted the superseded `82:2` through `87:2` range from Figma, and rebased repo governance docs onto the clean canonical range `89:2` through `94:2` with final coverage `18 / 10 / 8` |
| 2026-03-13 | `KAN-83` | Legacy route-family cleanup and stale design artifact pruning | removed obsolete legacy route-family capture references from repo-side design artifacts | Pruned obsolete catalog assets and governance references so removed legacy route families are no longer treated as live or pending official catalog surfaces |
| 2026-03-13 | `KAN-84` | Detailed screen-to-runtime reconciliation docs | `65:2` through `69:2`, `auth__*`, `chat__*`, `policy__*`, `premium__*`, callback route gap notes | Added a card-level mapping registry between canonical Figma pages and actual Flutter runtime surfaces, linked the new registry from the Figma governance docs, and documented that `/auth/callback` still exists in runtime but is not represented as a dedicated current-state Figma card |
| 2026-03-13 | `KAN-82` | Official catalog deep cleanup and current-code reconciliation | `00 Cover & Governance`, `99 Archive`, design governance docs, cleanup runbook, stale page inventory | Rebased the repo-side Figma contract onto the confirmed canonical page roots `64:2` through `69:2`, replaced stale `/fortune/interactive/*` governance copy with `/chat`-centered current-state wording, and recorded the exact stale roots still present in the official file for manual deletion |
| 2026-03-12 | `KAN-80` | Current-state chat surface expansion | `chat__home__general_default`, `chat__home__curiosity_default`, `chat__survey__fortune_step`, `chat__result__fortune_complete`, `chat__onboarding__character_intro`, `premium__insight__default`, removed stale `utility__manseryeok__default`, updated component groups | Expanded the repo-backed catalog to reflect `/chat` internal experiences and the retained premium route, while dropping the non-routable manseryeok screen from current-state coverage so the official Figma file can be re-appended from the updated manifest |
| 2026-03-11 | `KAN-70` | Reused retained runtime load paths | `chat__character__luts`, `utility__manseryeok__default`, retained provider-backed capture sources | Extended the retained manifest/source registry so character chat and manseryeok provider state changes remain covered by the official catalog guard |
| 2026-03-11 | `KAN-71` | Figma guard parity for contracted catalog | `chat__profile_sheet__default`, `account__deletion__auth_gated`, retained catalog governance docs | Added explicit placeholder triage metadata, corrected retained surface counts in the docs, and documented the MCP operator workflow required by the sync guard |
| 2026-03-11 | `KAN-71` | Product contraction for legacy catalog groups | active catalog reduced to `10 Entry / Auth / Onboarding`, `20 Chat / Character`, `80 Admin / Policy / Utility`; removed commerce/history/profile/more/trend/wellness references | Rewrote the repo manifest and design source docs to the retained surfaces only, and recorded the manual Figma page delete list for the superseded legacy page range |
| 2026-03-11 | `KAN-67` | Manual Figma rename runbook for refreshed append range | appended official-file pages `32:2` through `43:2`, governed anchor naming scope | Added a node-id-based manual rename runbook so the refreshed MCP-appended catalog pages can be normalized page-by-page without touching the older pre-refresh page set first |
| 2026-03-11 | `KAN-67` | MCP catalog append refresh helper | `00 Cover & Governance` through `99 Archive`, helper operator path | Regenerated catalog HTML, appended refreshed pages into the official file through MCP existing-file capture, corrected the appended cover card to `36 live / 26 placeholder`, and recorded remaining manual rename cleanup |
| 2026-03-11 | `KAN-67` | Live MCP audit of official catalog parity | Cover governance counts, legacy section/layer names, representative signup screen card | Verified repo parity, recorded stale `35 live / 26 placeholder` cover card, and documented remaining manual Figma rename cleanup |
| 2026-03-11 | `KAN-65` | Figma layer governance + MCP sync contract | `section__00__cover_governance` through `section__99__archive`, all governed `screen_card__*`, component groups, sync guard | Added canonical layer naming docs, manifest contract fields, catalog export attributes, and guard checks for MCP-managed governance |
| 2026-03-10 | `KAN-60` | Official catalog formalization | `10 Entry / Auth / Onboarding` through `99 Archive` | Created the single official catalog file and aligned repo docs |
| 2026-03-10 | `KAN-61` | Wellness route coverage | `wellness__landing__default`, `wellness__meditation__default`, `Wellness Focus Blocks` | Added `75 Wellness` to the official file and synced docs |
| 2026-03-10 | `NO-JIRA` | Figma sync automation guard | `figma_capture_manifest` governance, changelog enforcement, CI drift reporting | Added automatic guard so route/UI changes cannot pass CI without a recorded Figma sync step |
