# 🔮 Fortune 운세 생성 가이드

> Fortune 앱의 운세 생성 시스템을 위한 통합 실무 가이드입니다.

## 📋 목차
1. [시스템 개요](#시스템-개요)
2. [운세 카테고리 및 타입](#운세-카테고리-및-타입)
3. [토큰 최적화 전략](#토큰-최적화-전략)
4. [기술 구현 가이드](#기술-구현-가이드)
5. [운세별 세부 전략](#운세별-세부-전략)
6. [품질 관리](#품질-관리)

---

## 🌟 시스템 개요

### 핵심 아키텍처
- **AI 모델**: GPT-4.1-nano (최신 모델)
- **캐싱**: Redis + 메모리 하이브리드
- **인코딩**: Unicode NFC 정규화
- **에러 처리**: 다단계 폴백 시스템

### 운세 생성 시나리오
1. **온보딩 완료**: 평생 운세 패키지 (1년 캐시)
2. **일일 갱신**: 종합 일일 운세 (24시간 캐시)
3. **사용자 요청**: 개별 운세 (1시간 캐시)

---

## 📊 운세 카테고리 및 타입

### 카테고리별 분류 (총 55개)

| 카테고리 | 개수 | 주요 타입 | 캐시 전략 |
|---------|------|-----------|-----------|
| **데일리** | 4 | daily, today, tomorrow, hourly | 24시간 |
| **전통·사주** | 6 | saju, tojeong, palmistry | 365일 |
| **연애·인연** | 10 | love, compatibility, marriage | 1-24시간 |
| **커리어·재물** | 5 | career, wealth, investment | 1-7일 |
| **행운 아이템** | 8 | lucky-color, lucky-number | 30일 |
| **건강·라이프** | 12 | 스포츠 시리즈, biorhythm | 1-7일 |

### 필수 정보 매트릭스

| 정보 | 데일리 | 전통 | 연애 | 커리어 | 행운 |
|------|--------|------|------|---------|------|
| 이름 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 생년월일 | ✅ | ✅ | ✅ | ✅ | ⭕ |
| 생시 | ⭕ | ✅ | ❌ | ❌ | ❌ |
| 성별 | ⭕ | ✅ | ✅ | ⭕ | ❌ |
| MBTI | ⭕ | ❌ | ⭕ | ⭕ | ❌ |

> ✅ 필수 | ⭕ 선택 | ❌ 불필요

---

## 💰 토큰 최적화 전략

### 묶음 요청 패키지

#### 1. 전통·사주 패키지 (5개 묶음)
```json
{
  "fortune_types": ["saju", "traditional-saju", "tojeong", "salpuli", "past-life"],
  "token_saving": "80%",
  "cache_duration": "365d"
}
```

#### 2. 일일 종합 패키지 (4개 묶음)
```json
{
  "fortune_types": ["daily", "hourly", "today", "tomorrow"],
  "token_saving": "75%",
  "cache_duration": "24h"
}
```

#### 3. 연애·인연 패키지 (4개 묶음)
- **솔로용**: love, destiny, blind-date, celebrity-match
- **연애중**: love, couple-match, chemistry, marriage
- **토큰 절약**: 70%

#### 4. 행운 아이템 패키지 (5개 묶음)
```json
{
  "fortune_types": ["lucky-color", "lucky-number", "lucky-items", "lucky-outfit", "lucky-food"],
  "token_saving": "85%",
  "cache_duration": "720h"
}
```

### 토큰 절약 효과
| 패키지 | 개별 요청 | 묶음 요청 | 절약률 |
|--------|-----------|-----------|---------|
| 전통·사주 | ~15,000 | ~3,000 | 80% |
| 일일 종합 | ~8,000 | ~2,000 | 75% |
| 연애·인연 | ~10,000 | ~3,000 | 70% |
| 행운 아이템 | ~6,000 | ~900 | 85% |

---

## 🔧 기술 구현 가이드

### 1. 운세 생성 서비스 사용법

```typescript
// 단일 운세 생성
const response = await enhancedFortuneService.generateFortune({
  userId: 'user123',
  fortuneType: 'daily',
  profile: userProfile,
  force: false // 캐시 사용
});

// 배치 운세 생성
const batchResults = await enhancedFortuneService.generateBatchFortunes(
  userId,
  ['daily', 'tarot', 'mbti'],
  userProfile
);

// 품질 평가
const quality = await enhancedFortuneService.evaluateFortuneQuality(
  fortuneData,
  'daily'
);
```

### 2. 공통 데이터 구조

```typescript
interface BaseFortuneInput {
  name: string;
  birth_date: string;
  birth_time?: string;
  gender?: 'male' | 'female';
  mbti?: string;
}

interface BaseFortuneResult {
  overall_score: number;
  summary: string;
  details: string;
  lucky_items?: {
    color?: string;
    number?: number;
    direction?: string;
    time?: string;
  };
  advice?: string;
  generated_at: string;
  cache_duration: string;
}
```

### 3. 에러 처리 및 폴백

```typescript
try {
  // 1차: 캐시 확인
  const cached = await cache.get(key);
  if (cached) return cached;
  
  // 2차: AI 생성
  const generated = await generateWithAI(params);
  if (generated) return generated;
  
  // 3차: 과거 데이터 활용
  const historical = await getHistoricalData(userId, fortuneType);
  if (historical) return adaptHistoricalData(historical);
  
  // 4차: 기본값 반환
  return getDefaultFortune(fortuneType);
} catch (error) {
  logger.error('Fortune generation failed', error);
  return getFallbackFortune(fortuneType);
}
```

---

## 🎯 운세별 세부 전략

### 📅 Daily (오늘의 운세) - 최우선
- **타겟**: 모든 연령층
- **수익화**: 무료 + 광고 / 프리미엄 상세분석
- **특징**: 매일 방문 유도, 푸시 알림
- **개선**: 위젯 개발, 개인화 알고리즘

### 🔮 Saju (사주팔자) - 고수익
- **타겟**: 30-50대
- **수익화**: 평생 리포트 49,000원 / PDF 19,000원
- **특징**: 전문적 분석, 높은 객단가
- **개선**: 용어 사전, 난이도 조절

### 💕 Love (연애운) - 바이럴
- **타겟**: 20-30대
- **수익화**: 월 구독 7,900원 / 이상형 분석
- **특징**: 높은 공유율, 재방문율
- **개선**: 매칭 알고리즘, 커뮤니티

### 💼 Career (취업운) - 시즌성
- **타겟**: 취준생, 이직준비자
- **수익화**: 면접날짜 추천, 기업궁합
- **특징**: 상/하반기 피크
- **개선**: 취업사이트 제휴

### 🎨 Lucky Items - 일상화
- **타겟**: 모든 연령
- **수익화**: 광고 중심, 브랜드 콜라보
- **특징**: 실용적, 일상 적용
- **개선**: SNS 템플릿, 제휴 상품

---

## 📈 품질 관리

### 성능 지표
- **캐시 히트율**: 80% 이상
- **AI 응답시간**: 2초 이하
- **에러율**: 0.1% 미만
- **사용자 만족도**: 4.5+ 유지

### 품질 체크리스트
- [ ] 한글 인코딩 정상 여부
- [ ] 응답 형식 일관성
- [ ] 개인화 수준 적절성
- [ ] 운세 내용 긍정성
- [ ] 실용적 조언 포함

### 모니터링
```typescript
// 토큰 사용량 추적
tokenTracker.track(userId, fortuneType, tokensUsed);

// 캐시 통계
const stats = await cacheManager.getStats();

// 품질 평가
const qualityScore = await evaluateQuality(fortune);
```

---

## 🚀 실행 우선순위

### Phase 1 (즉시)
1. **Daily, Love, Saju** 집중 최적화
2. 묶음 요청 API 구현
3. 캐싱 시스템 고도화

### Phase 2 (1개월)
1. 프리미엄 기능 출시
2. 푸시 알림 시스템
3. A/B 테스트 플랫폼

### Phase 3 (3개월)
1. AI 개인화 엔진
2. 위젯 개발
3. 해외 시장 진출

---

## 📊 KPI 목표

### 사용자 지표
- DAU: 10만명
- MAU: 100만명
- 리텐션 (7일): 40%

### 수익 지표
- ARPU: 500원
- 프리미엄 전환율: 5%
- 월 매출: 1억원

### 기술 지표
- API 응답시간: 200ms
- 토큰 사용량: 65% 절감
- 캐시 효율: 80%

---

*최종 업데이트: 2025년 7월 11일*